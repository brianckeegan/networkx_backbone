Graph Comparison Gallery
========================

Comparisons of original graphs against score-then-filtered backbones.

Examples warn when filtered edge counts are unchanged from the original graph,
which indicates thresholds or method settings should be re-validated.
